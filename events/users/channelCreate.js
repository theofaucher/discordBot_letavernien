module.exports = async (client,channel) => {
    

    // const fetchedLogs = await channel.guild.fetchAuditLogs({
    //     limit:1,
    //     type:'CHANNEL_CREATE'
    // });


    // console.log(channel.guild.members.cache.get(fetchedLogs.entries.first().executor.id))

}

            // console./Elog(fetchedLogs.entries.first().executor.id)
        // .find(element => element == '563780601807896586')./*members.cache.find(element => element == fetchedLogs.entries.first().executor.id)
        // .cache.find(element => element == '563780601807896586').roles.cache.find(element => element == '563780818233720832').guild.roles.cache.find(element => element == '563780818233720832')
        // console.log(client.cache)
                // console.log(`channelCreate: ${channel}`);
        // console.log(client.channels.cache.find(element => element == channel).guild.members);
        